"use client";import{useEffect,useState}from"react";import{apiFetch}from"../lib/api-client";import"./demo-portfolio.css";
export function DemoPortfolioLoader({eligible}:{eligible:boolean}){const[state,setState]=useState<"checking"|"ready"|"loading"|"installed"|"error">("checking"),[message,setMessage]=useState("");useEffect(()=>{if(!eligible){setState("ready");return}apiFetch("/api/v1/demo-portfolio").then(r=>r.json()).then(x=>{if(x.installed){setState("installed");setMessage(`Dataset ${x.installation?.dataset_version||"DEMO-2026.1"}`)}else install()}).catch(()=>setState("error"))},[eligible]);async function install(){setState("loading");const r=await apiFetch("/api/v1/demo-portfolio",{method:"POST",headers:{"content-type":"application/json"},body:JSON.stringify({type:"install"})}),b=await r.json();if(!r.ok){setState("error");setMessage(b.error||"Não foi possível instalar.");return}setState("installed");setMessage(`${b.records||65} registos · ${b.datasetVersion||"DEMO-2026.1"}`);setTimeout(()=>window.location.reload(),900)}if(!eligible)return null;return <section className={`demo-loader ${state}`}><i>{state==="installed"?"✓":state==="error"?"!":"◇"}</i><span><small>AMBIENTE DE DEMONSTRAÇÃO</small><b>{state==="loading"?"A instalar dados integrados…":state==="installed"?"Portefólio demonstrativo instalado":state==="error"?"Instalação requer atenção":"A preparar demonstração"}</b><p>{message||"Finance, HCM, Payroll, Workforce, Dashboard e Management Report com dados reconciliados."}</p></span>{state==="error"&&<button onClick={install}>Tentar novamente</button>}</section>}
